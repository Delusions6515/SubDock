import 'package:flutter/widgets.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  // ponytail: mobile runtime is deferred; replace this entry when it exists.
  runApp(const SizedBox.shrink());
}
